<h1>id:
<%= @tasklist.id %>
のタスク編集ページ</h1>

<%= form_for(@tasklist) do |f| %>

<%= f.label :content, "タスク" %>

<%= f.text_field :content %>

<%= f.submit "登録" %>

<% end %>

<%= link_to "一覧に戻る", tasklists_path %>